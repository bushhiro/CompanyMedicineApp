import 'package:flutter/material.dart';

/// Единая цветовая палитра для всего проекта.
class AppColors {
  // Основные цвета
  static const Color secondaryTextColor = Color(0xFFd2ddeb);          // Главный (фон карточек, AppBar и т.д.)
  static const Color backgroundColor = Color(0xFFE1EAF4);  // Фон приложения
  static const Color buttonColor = Color(0xFF0C1F4D);      // Цвет кнопок
  static const Color primaryTextColor = Colors.black;
  static const Color primaryColor = Color(0xffffffff);      // Основной цвет текста
  static const Color hintColor = Color(0xFF9E9E9E);
  static const Color titleColor = Color(0xFF0C1F4D);
  // Подсказки, вторичный текст

  // Дополнительные оттенки
  static const Color borderColor = Color(0xFFE0E0E0);      // Светло-серый для рамок
  static const Color extraButtonColor = Color(0xFF2196F3);      // Голубой
  static const Color errorColor = Color(0xFFD32F2F);       // Ошибки
  static const Color successColor = Color(0xFF4CAF50);     // Успешные действия
  static const Color warningColor = Color(0xFFFFA000);     // Предупреждения
  static const Color patientCardStatusColor = Color(0xFFE5EEF9);
}